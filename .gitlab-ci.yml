stages:
  - lint

flake8:
  stage: lint
  image: python:3.12
  before_script:
    - python -m pip install --upgrade pip
    - pip install -r requirements/prod.txt
    - pip install flake8
  script:
    - flake8 .
  only:
    - merge_requests
    - main